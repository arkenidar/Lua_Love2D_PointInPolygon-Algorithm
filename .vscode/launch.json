{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug LOVE2D (Win. BUNDLED)",
            "type": "lua-local", // tomblind.local-lua-debugger-vscode
            "request": "launch",
            "program": {
                "command": "${workspaceFolder}\\runtime-love2d\\love.exe"
            },
            "args": ["${workspaceFolder}"],
            "cwd": "${workspaceFolder}"
        }
    ]
}